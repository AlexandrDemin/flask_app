<?xml version="1.0" encoding="UTF-8"?>
<sitemapindex xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    {% for i in range(1, sitemapCount+1) %}
        <sitemap>
            <loc>{{urlRoot}}/sitemap{{i}}.xml</loc>
            <lastmod>{{lastMod}}</lastmod>
        </sitemap>
    {% endfor %}
</sitemapindex>